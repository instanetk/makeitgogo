import { Box } from '@mui/material';

const Footer = () => {
  return (
    <Box
      sx={{
        height: '100px',
        display: 'flex',
        justifyContent: 'center',
        marginTop: '50px',
        backgroundColor: '#eee',
      }}></Box>
  );
};

export default Footer;
